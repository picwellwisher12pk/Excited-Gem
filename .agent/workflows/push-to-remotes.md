---
description: Push the current branch or all branches to origin and github remotes
---

1. Push current branch to origin
   - `git push origin HEAD`

2. Push current branch to github
   - `git push github HEAD`

3. (Optional) Push all branches to origin
   - `git push origin --all`

4. (Optional) Push all branches to github
   - `git push github --all`
